# Generierter App-Datensatz

Dieses Verzeichnis enthält bei lokalen und Release-Builds optional
`charging-de.sqlite3`. Die Datei wird durch
`tooling/prepare_app_dataset.sh` erzeugt und nicht in Git gespeichert.

Fehlt sie, verwendet die App die kleine versionierte Contract-Fixture.
